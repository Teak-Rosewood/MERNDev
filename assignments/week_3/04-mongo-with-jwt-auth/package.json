{
  "name": "03-mongo",
  "version": "1.0.0",
  "description": "### Description Same as the last assignment but you need to use jwts for authentication. We have introduced the signgin endpoints for both users and admins. For this one, in every authenticated requests, you need to send the jwt in headers (Authorization : \"Bearer <actual token>\"). You need to use mongodb to store all the data persistently.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.23.3",
    "bcrypt": "^5.1.1",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "package.json": "^0.0.0",
    "supertest": "^6.3.3",
    "ts-node": "^10.9.2"
  }
}
